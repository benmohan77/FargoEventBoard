<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/eventBlue"
    android:gravity="center|center_horizontal"
    tools:context=".fragments.LoginFragment">

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="@dimen/string_title" />

    <EditText
        android:id="@+id/username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/title"
        android:layout_centerInParent="true"
        android:layout_marginLeft="@dimen/input_lr_margin"
        android:layout_marginTop="@dimen/input_margin"
        android:layout_marginRight="@dimen/input_lr_margin"
        android:background="@drawable/input_background"
        android:ems="10"
        android:inputType="textPersonName"
        android:padding="10dp"
        android:text="@string/username"
        android:textColor="@color/white" />

    <EditText
        android:id="@+id/password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/username"
        android:layout_centerInParent="true"
        android:layout_marginLeft="@dimen/input_lr_margin"
        android:layout_marginTop="@dimen/input_margin"
        android:layout_marginRight="@dimen/input_lr_margin"
        android:background="@drawable/input_background"
        android:ems="10"
        android:inputType="textPassword"
        android:text="@string/password"
        android:padding="10dp"
        android:textColor="@color/white" />
        
    <Button
        android:layout_below="@id/password"
        android:id="@+id/login_btn"
        android:padding="10dp"
        android:text="@string/login"
        android:layout_marginTop="@dimen/input_margin"
        android:layout_marginLeft="@dimen/input_lr_margin"
        android:layout_marginRight="@dimen/input_lr_margin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/eventDarkBlue"
        />

</RelativeLayout>